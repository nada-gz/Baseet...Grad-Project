# Egyptian Arabic Kids Explainer — Python dependencies
# Install with:  pip install -r requirements.txt
#
# System requirements (must be installed separately):
#   - FFmpeg  : brew install ffmpeg   (macOS) | apt install ffmpeg (Ubuntu)
#   - Node.js : https://nodejs.org  (v18+)
#
# Node.js packages (auto-installed via npm inside remotion-composer/):
#   Run once:  cd remotion-composer && npm install
#
# Required API keys in backend/.env:
#   GROQ_API_KEY      — Groq LLM for fast script generation (https://console.groq.com)
#   PIXABAY_API_KEY   — Royalty-free background video clips (https://pixabay.com/api/docs/)
#   HF_TOKEN          — HuggingFace token for NileTTS Egyptian Arabic speech generation

# Loads .env file
python-dotenv

# HTTP requests (Groq API calls)
requests

# NileTTS — calls niletts-tts/niletts-api HuggingFace Space for Egyptian Arabic TTS
gradio_client
